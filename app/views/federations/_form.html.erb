<%= form_for(@federation) do |f| %>
  <% if @federation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@federation.errors.count, "error") %> prohibited this federation from being saved:</h2>

      <ul>
      <% @federation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">
	  <%= f.label :name %><br />
	  <%= f.text_field :name %>
	</div>
	<div class="field">
	  <%= f.label :thumbnail %><br />
	  <%= f.text_field :thumbnail %>
	</div>
	<div class="field">
	  <%= f.label :ip %><br />
	  <%= f.text_field :ip %>
	</div>
	<div class="field">
	  <%= f.label :site %><br />
	  <%= f.text_field :site %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
