<%= form_for(@white_list) do |f| %>
  <% if @white_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@white_list.errors.count, "error") %> prohibited this IP from being saved:</h2>

      <ul>
      <% @white_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip %><br>
    <%= f.text_field :ip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
